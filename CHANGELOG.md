# 바뀐 것

[Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 형식을 따르고,
판번호는 [유의적 버전](https://semver.org/lang/ko/)을 따른다.

`1.0.0` 이 **처음 공개 배포한 판**이다 — 2026-09-26,
<https://convertors.tkddls8848.workers.dev>. 그 아래 `0.1.0` 은 공개하지 않은 판으로,
이 변환기가 제 저장소를 갖게 된 날을 가리킨다.

기능이 아니라 **쓰는 사람에게 무엇이 달라지는가**로 적는다. 커밋 하나하나가 궁금하면
`git log` 를 보라.

## [1.0.0] - 2026-09-26

**처음으로 공개 배포했다** — <https://convertors.tkddls8848.workers.dev>.

이 묶음은 기능을 늘리지 않는다. **사내 도구를 공개 제품으로 내놓을 수 있게 만드는
일**이다 — 법적 고지, 검색 노출, 접근성, 보장 브라우저, 성능 약속. 판번호를 1.0.0
으로 올린 것은 기능이 커져서가 아니라 **남에게 내놓을 수 있는 상태가 되었다**는
뜻이다. 0.x 는 그 전, 저장소 안에만 있던 판이다.

### 더함

- **라이선스와 제3자 고지.** 저장소가 [`LICENSE`](LICENSE)(상용 비공개,
  All rights reserved)를 갖는다. 함께 배포되는 구성요소 29건의 고지를
  [`THIRD-PARTY-NOTICES.md`](THIRD-PARTY-NOTICES.md) 에 모으고 원문을
  `web/public/licenses/` 로 함께 올린다. 목록은 손으로 적지 않고
  `web/scripts/gen_licenses.mjs` 가 `node_modules` 에서 뽑는다 — 손으로 적은 고지는
  의존성을 올릴 때 조용히 어긋나고, 어긋난 고지는 없느니만 못하다. 직접 의존성뿐
  아니라 **패키지가 제 안에 함께 싣는 남의 자료**(PDF.js 의 cmap·기본 글꼴·wasm·ICC)와
  심는 한글 글꼴까지 따라간다 — 그것들도 실제로 배포물에 실려 나간다
- **화면 아래 고지 링크.** 쓰는 사람이 무엇을 빌려 쓰는 도구인지 화면에서 바로 볼 수
  있다. 고지 자료가 실리지 않은 배포에서는 **링크를 아예 내지 않는다** — 빈 링크를
  약속하지 않는 편이 낫다
- **검색에 뜬다.** `robots.txt` 의 `*` 그룹이 `Allow: /` 가 되고 `noindex` 가
  사라졌다. **AI 학습·요약용 수집기 차단 목록은 그대로다** — 검색에 뜨는 것과 남의
  말뭉치에 들어가는 것은 별개이고, 후자는 한 번 들어가면 되돌릴 수 없다
- **공유하면 미리보기가 나온다.** 설명·Open Graph·Twitter 카드, 그리고 실제 파비콘과
  1200×630 미리보기 그림. 전에는 탭 아이콘이 빈 것(`data:,`)이었다
- **배포 주소를 `VITE_SITE_URL` 로 받는다.** 없으면 canonical·og:url·`sitemap.xml` 을
  **아예 내지 않는다.** 그럴듯한 주소를 지어내면 검색 엔진이 남의 주소를 정본으로
  알기 때문이다. 값이 주소로 읽히지 않으면 빌드를 세운다
- **접근성 검사**(`web/scripts/check-a11y.mjs`) — 이름 없는 조작 요소, 건너뛴 제목
  단계, 대비가 모자란 글자를 실제 브라우저에서 찾는다
- **번들 예산 검사**(`web/scripts/check-bundle.mjs`) — 첫 화면이 받는 JS 를 실제
  브라우저의 네트워크로 재고, 예산을 넘거나 무거운 엔진(fontkit·PDF.js·
  SheetJS 워커)이 첫 화면에 딸려오면 **0 이 아닌 값으로 죽는다.** "고른 도구만 받아
  온다"는 설계가 조용히 무너지는 것을 막는다
- **CI**(`.github/workflows/ci.yml`) — 밀어 넣을 때마다 타입 검사·단위 테스트·빌드가
  돌고, Chromium·Firefox·WebKit 세 엔진에서 화면 검사가 돌고, 배포 모양 그대로
  띄운 뒤 헤더·자산·CSP 검사가 돈다
- **[`SECURITY.md`](SECURITY.md)** — 취약점 신고 창구, 기대 응답 시간, 공개 시점
  합의. 서버가 없는 도구의 위협 모형(CSP 우회·zip-slip·압축폭탄·신뢰할 수 없는 파일
  파싱)을 따로 적었다
- **스테이징은 색인을 막고 굽는다** (`VITE_NOINDEX=1`). 프로덕션과 같은 dist 를
  올리므로 그냥 두면 같은 내용이 두 주소에 뜨고, 하필 스테이징이 색인되면 쓰는
  사람이 낡은 판을 본다. robots.txt·X-Robots-Tag·meta 셋을 함께 켜고 sitemap 은
  내지 않되, **canonical 은 프로덕션을 가리킨 채로 둔다.** 검사도 같은 변수를 읽어
  두 모드에서 서로 반대를 단언한다 — 안 그러면 프로덕션에 noindex 를 올려도 검사가
  통과한다
- **이 파일**과 제품 문서로 다듬은 [`README`](README.md)

### 고침

- **배포물에서 카피레프트를 걷어냈다.** PDF.js 가 함께 싣는 Liberation 글꼴 4개가
  GPLv2 + 글꼴 예외라, 상용 비공개로 내놓는 배포물에 싣지 않기로 했다. 빼기 전에
  영향을 실제로 쟀다 — **보는 사람의 컴퓨터에 그 글꼴이 있으면 PDF.js 는 이 자료를
  아예 요청하지 않는다**(요청 0건, 산출물 바이트 동일). 없는 컴퓨터에서 비임베디드
  sans 를 만났을 때만 브라우저 기본 sans 로 그려지고, 그때 픽셀 0.92% 가 달라졌으며
  글자는 그대로 읽혔다. Times 와 Courier 는 같은 폴더의 Foxit(PDFium BSD)가 계속
  맡는다. 무엇이 실려 나가는지는 이제 `web/pdf-assets.json` 하나가 정하고 **빌드와
  고지 생성이 둘 다 그 파일을 읽는다** — 목록을 두 곳에 적으면 배포물과 고지가
  조용히 어긋난다
- **키보드와 낭독기로 쓸 수 있다.** 도구를 고르면 그 도구의 제목으로 초점이 옮겨
  가 무엇이 열렸는지 낭독된다(고르던 자리까지 함께 움직이지는 않는다 — 그러면
  위치를 잃는다). 고른 도구 영역에 이름이 붙고, 제목 단계가 건너뛰지 않게 정리했고,
  이름표가 없던 입력 칸에 이름표를 붙였다
- 글자 대비를 어두운 화면에서 읽히는 값으로 맞췄다

### 확인함

- **Chrome · Firefox · Safari 세 엔진에서 검사 12회 전부 통과**(Chromium 153,
  Firefox 155, WebKit 26.6). 검사 스크립트가 `CHECK_BROWSER` 로 엔진을 고를 수 있게
  되었다. **한계도 함께 적는다** — Playwright WebKit 은 실제 Safari 기기가 아니고,
  이 결과가 macOS·iOS Safari 앱에서의 동작을 보장하지는 않는다.
  기록은 `.cache/cross-browser-report.md` 에 있다
- 단위 테스트 413건(파일 30개) 통과

## [0.1.0] - 2026-09-24

저장소가 지금 모습을 갖춘 날. 커밋 둘이다.

### 더함

- **변환기가 제 저장소를 갖는다.** 견적서 작성기와 한 화면에 탭으로 서 있던 것을
  떼어 냈다 — 서로를 부르지 않는 기능이 한 배포에 묶여 있을 이유가 없었다.
  변환 논리·화면·테스트는 한 줄도 고치지 않고 옮겼고, 셸(`web/index.html` ·
  `web/src/main.ts` · `web/src/styles.css`)만 새로 지었다
- **Cloudflare 에 올릴 수 있다.** 올라가는 것은 **정적 자산뿐이다** —
  `wrangler.jsonc` 에 Worker 스크립트가 없다. 받을 서버가 아예 없으면 파일을 밖으로
  보낼 방법도 없다. 없는 주소에는 404 로 답한다(화면이 하나뿐이라, 오타를 친 사람에게
  멀쩡한 화면을 보여 주지 않는다)
- **CSP 가 약속을 지키게 한다.** `web/public/_headers` 의
  `connect-src 'self'` — 파일이 나가지 않는다는 말이 코드의 선의가 아니라 브라우저가
  지키는 규칙이 된다
- **배포 모양 그대로 보는 검사**(`web/scripts/check-deploy.mjs`) — 개발 서버에는
  CSP 가 없고 자산도 번들되기 전이라 배포에서만 깨지는 것을 잡지 못한다.
  이것을 만들다 알게 된 것 하나: 기존 검사들이 산출물을 `fetch(blob:)` 으로 읽는데
  CSP 가 그것을 막는다. **막는 것이 맞아서** CSP 를 풀지 않고, 브라우저가 실제로
  내려받은 파일을 디스크에서 읽는 방식으로 바꿨다
- 심을 한글 글꼴을 받아 오는 `web/scripts/fetch_fonts.py`. **못 받아도 빌드를 세우지
  않는다** — 그때는 라틴 기본 글꼴과 "내 글꼴 불러오기" 로 물러나고 화면이 그 사실을
  적는다

### 뺌

- **HWP(5.0) → PDF 변환 서버.** 공개할 수 없어 내렸다. 파일을 브라우저 밖으로
  내보내는 유일한 예외였고, 그 하나 때문에 도구 전체의 약속이 약해졌다
